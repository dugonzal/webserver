
name: Hello World

on:
  push:
    branches:
      - D-0 

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Hello World
      run: sudo apt install zsh &&  make re
